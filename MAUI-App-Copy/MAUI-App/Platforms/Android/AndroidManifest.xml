<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android" package="com.companyname.notes">
    <!-- Allow camera access for taking photos -->
    <uses-permission android:name="android.permission.CAMERA" />

    <application android:label="Notes" android:theme="@style/Maui.SplashTheme">
        <activity android:name="crc64b5f8f1fdb09c0534.MainActivity"
                  android:exported="true"
                  android:label="Notes"
                  android:theme="@style/Maui.MainTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>
